# Meridian Skill Cards

`dev/skills/` only contains Meridian-specific hazard cards. Shared commit, PR, React, IndexedDB, and service-worker cards live under `dev/foundation/core/skills/` or `dev/foundation/platforms/web-react/skills/` and are read directly through `dev/README.md`.

## Current cards

- `offline-time-resolution.md`: deterministic parity between online resolution, background suspension, and return-from-offline resolution.

Add a local skill only for a recurring Meridian-specific recipe or hazard that cannot remain precise as a foundation rule.
